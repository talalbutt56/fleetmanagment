{
  "name": "fleet-management-backend",
  "version": "1.0.0",
  "description": "Production-ready backend for Fleet Management",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build required for Node.js' && exit 0",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
"dependencies": {
  "express": "^4.18.2",
  "mongoose": "^8.0.3",
  "body-parser": "^1.20.2",
  "dotenv": "^16.3.1",
  "cors": "^2.8.5"  // Add this line
}
